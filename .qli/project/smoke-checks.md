# Smoke Checks — Squirrelmail

Generated by onboard-project.sh v1.1.2 on 2026-07-04T03:09:19Z

## Checks

| Check | Result | Notes |
|---|---|---|
| Container start | pass | docker compose up starts all three services |
| App port binding | pass | 127.0.0.1:20110 only (20100 removed) |
| DB port exposure | n/a | No database required |
| Upstream health | pass | Login page renders at /src/login.php |
| Route registration | pass | Route registered via /usr/local/bin/route-project (no sudo) |
| Caddy route active | pass | Caddy reloaded successfully |
| External HTTPS login | pass | https://squirrelmail.qlidemo.com/src/login.php HTTP 200 |
| External configtest | pass | Blocked ($allow_remote_configtest=false. Returns error page only.) |
| HTTP redirect | blocked | HTTP redirects to qlinnovations.com, not squirrelmail.qlidemo.com |
| IMAP login (direct) | pass | Dovecot first_valid_uid=101 fix applied. testuser/testpass login succeeds. |
| IMAP login (app) | pass | SquirrelMail redirect.php authenticates successfully via IMAP. |
| Post-login redirect | blocked | After login, redirects to qlinnovations.com (Caddy reverse-proxy issue). |
| Mailbox functional | blocked | Cannot reach due to post-login redirect blocker. |
| SMTP capture functional | blocked | Cannot test due to redirect blocker. |
| Route acceptance-ready | blocked | HTTPS login works, IMAP works. Post-login redirect is the remaining blocker. |

## Health paths to probe

- http://127.0.0.1:20110/src/login.php — login page
- http://127.0.0.1:20110/src/configtest.php — configuration test

## IMAP test credentials

| Username | Password |
|---|---|
| testuser | testpass |
| devbox | devbox |

## SMTP capture

Outbound mail is captured by Mailpit (internal service `smtp:1025`).
No auth required. All mail stays within the DevBox Docker network.

## Results

Completed: 2026-07-04

## Result

Runtime started  : true
Route status     : route-finalized
Caddy status     : reloaded
Completed at     : 2026-07-04

## Route finalization result

- Completed at    : 2026-07-04T06:37:24Z
- Upstream status : http-302 (local upstream healthy)
- Route status    : registered
- Caddy status    : reloaded
- No-sudo tool    : /usr/local/bin/route-project

## Known blockers

1. **HTTP redirect**: `http://squirrelmail.qlidemo.com/` redirects to `https://qlinnovations.com/` instead of `https://squirrelmail.qlidemo.com/`.
   - Root cause: Global Caddy wildcard `http://*.qlidemo.com { redir https://qlinnovations.com{uri} }` in `/etc/caddy/Caddyfile`
   - Fix: Change `qlinnovations.com` to `{host}` in the wildcard redirect
   - Requires: DevBox operator with sudo. Full diagnosis: `.qli/project/caddy-redirect-correction.md`
2. **Post-login redirect**: After successful IMAP authentication, browser redirects to `qlinnovations.com/src/webmail.php` instead of `squirrelmail.qlidemo.com/src/webmail.php`. Caddy reverse-proxy issue.
3. **IMAP login**: ✅ Fixed in this pass (`first_valid_uid=101` in dovecot.conf).
