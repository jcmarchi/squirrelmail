slice_id: "modernization-login-seam-001"
title: "Login Page Presentation Seam Extraction"
mode: "modernization"
objective: "Extract inline JavaScript onload construction from src/login.php into isolated sqm_login_build_onload_script() function. First modernization slice per source seam map Section 1 (Login Page Presentation). Behavior-preserving refactor."
approved_scope:
  include:
    - "src/login.php"
    - ".qli/project/evidence-log.md"
    - ".qli/project/current-slice.yml"
    - ".qli/project/context-packet.yml"
  exclude:
    - "src/redirect.php"
    - "src/webmail.php"
    - "src/right_main.php"
    - "src/read_body.php"
    - "src/compose.php"
    - "functions/**"
    - "class/**"
    - "plugins/**"
    - "themes/**"
    - "config/**"
    - "include/**"
    - "docker/**"
    - "docker-compose.yml"
triggered_foundations:
  - acd
expected_evidence:
  core: "First source seam extracted. Function isolation pattern proven on login page. No auth/session/IMAP/password changes."
  ui: "Login page renders identically. Frameset UI preserved post-login."
  tests: "Manual browser validation: T1 (login page), T2 (login + mailbox), T16 (configtest blocked)."
  docs: "evidence-log.md updated with slice entry."
risk_level: "low"
validation_tier: "tier2-targeted"
