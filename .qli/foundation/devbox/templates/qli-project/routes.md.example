# Routes — Example Template
# Copy this file to .qli/project/routes.md in the managed project.
# Replace all placeholder text. Do not leave placeholder text in a completed file.
# Use <PROJECT_SLUG>.<DOMAIN> placeholders for hostnames unless the operator
# explicitly approves recording live route names for a private/internal project.
# This file is safe to commit to project version control. Do not include secrets or TLS keys.

# Routes

Project: `<PROJECT_SLUG>`
Recorded: `<DATE>`

---

## Route intent

<!-- Describe what routes this project needs and why.
     Use <PROJECT_SLUG>.<DOMAIN> style placeholders for hostnames. -->

- Primary app: `<PROJECT_SLUG>.<DOMAIN>` → app container on `QLI_APP_PORT`
- API: `<PROJECT_SLUG>.<DOMAIN>/api/` → proxied from app container (or separate upstream if applicable)

---

## Caddy registration status

<!-- one of: not-registered / registered / deferred -->

not-registered

---

## Access mode

<!-- one of: public / private / internal / none -->

public

---

## Upstream target

<!-- The Caddy upstream target for this project.
     Use the port variable name for clarity; resolve from port-block.env. -->

```
http://127.0.0.1:${QLI_APP_PORT}
```

---

## API routing model

<!-- Describe how API routes are handled. Choose one: -->

- [ ] Proxied from the main app domain (same upstream)
- [ ] Separate subdomain (`api.<PROJECT_SLUG>.<DOMAIN>`)
- [ ] Separate upstream target (`http://127.0.0.1:${QLI_API_PORT}`)
- [ ] None

---

## Public exposure notes

<!-- Any notes about what is and is not publicly exposed.
     Example: /admin paths are blocked. /api/internal paths require client auth. -->

<!-- Add notes here -->

---

## Caddy validation status

<!-- one of: not-run / passed / failed / not-applicable -->

not-run

<!-- Record date of last `caddy validate` run if applicable. -->
