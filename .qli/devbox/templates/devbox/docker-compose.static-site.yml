services:
  web:
    build:
      context: .
      dockerfile: infra/docker/devbox/static-nginx.Dockerfile
    container_name: <SLUG>-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:<APP_PORT>:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
