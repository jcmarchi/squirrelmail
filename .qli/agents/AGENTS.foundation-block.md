# QLi Buildline Legacy AGENTS Block

This file is retained for compatibility with earlier Buildline packages that composed QLi-specific instructions directly into `AGENTS.md`.

For new skeletons, use root `QLI.md` instead.

Minimum `AGENTS.md` Buildline reference:

```markdown
This repository uses **QLi Buildline**.

Before planning, editing, testing, refactoring, or reporting, read and follow `./QLI.md`.
```

Do not paste the full Buildline doctrine into `AGENTS.md` unless a tool requires that workaround and the project owner approves it.
